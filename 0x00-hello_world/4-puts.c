#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints "Programming is like building a multilingual puzzle"
 * with a double quote in the beginning.
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n);
	return (0);
}
