#include <stdio.h>
/**
 *main - main entry point
 *return : always 0 (success)
 */
int main(void)
{
	/* return is always 0 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
