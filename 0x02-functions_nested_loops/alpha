#include "main.h"
/**
 * print_alphabet - prints the alphabe
 * description - prints alphabetsp
 * Return: Always 0.
 */

void print_alphabet(void)
{
	char a;

	for (a = 'a' ; a <= 'z' ; a++)
	{
		putchar(a);
	}
	putchar('\n');
}
