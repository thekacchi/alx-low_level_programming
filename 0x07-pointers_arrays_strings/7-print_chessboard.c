#include "main.h"

/**
  *print_chessboard - main entry point
  *a: parameter to be operated
  *Return: always 0
  */

void print_chessboard(char (*a)[8])
{
	int i, n;

	for (i = 0 ; n < 0; n++)
		_putchar(a[i][n]);
	_putchar('\n');
}
