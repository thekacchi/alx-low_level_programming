#include <stdio.h>
#include <string.h> 

/**
  *int _strlen_recursion - main entry point
  *@s: parameter to be operated
  *return: always 0
  */

int _strlen_recursion(char *s)
{
	int lenghtt;

	lenghtt = strlen(s);

	return (lenghtt);
}
