#include main.h
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    int r;

    r = is_palindrome(level);
    printf(%dn, r);
    r = is_palindrome(redder);
    printf(%dn, r);
    r = is_palindrome(test);
    printf(%dn, r);
    r = is_palindrome(step on no pets);
    printf(%dn, r);
    return (0);
}
